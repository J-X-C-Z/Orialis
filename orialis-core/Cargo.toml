[package]
name = "orialis-core"
version = "0.1.0"
edition = "2021"
description = "Orialis domain primitives and service metadata"
license = "UNLICENSED"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
